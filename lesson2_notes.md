# 第 2 课：数组 (Array)

## 核心概念

数组是内存中**连续存放**的一串相同类型的数据。最大优势是**随机访问 O(1)**——知道下标就能直接跳到对应位置：

```
地址(arr[i]) = 起始地址 + i × 每个元素大小
```

这个计算不用循环，硬件直接算，所以是 O(1)。

## 各操作复杂度

| 操作 | 时间复杂度 | 说明 |
|------|--------|------|
| 按下标访问 `arr[i]` | O(1) | 地址直接计算 |
| 末尾插入（有空间） | O(1) | 放在最后一位即可 |
| 末尾删除 | O(1) | 缩一下边界即可 |
| 头部/中间插入 | O(n) | 腾位置，后面元素全部后移 |
| 头部/中间删除 | O(n) | 后面元素全部前移 |
| 按值查找（无序） | O(n) | 一个个找 |
| 按值查找（有序） | O(log n) | 可以二分查找 |

## 内存模型

```
数组: [10, 20, 30, 40, 50]
地址: 0x100  0x104  0x108  0x10C  0x110
      (假设 int = 4 字节)

要访问 arr[3]:
地址 = 0x100 + 3 × 4 = 0x10C  → 直接取到 40
```

## 为什么插入是 O(n)？

```
插入 99 到位置 1:
[10, 20, 30, 40, 50, _ ]
       ↓ 每个元素后移一格
[10,  _, 20, 30, 40, 50]
       ↓
[10, 99, 20, 30, 40, 50]  ← 腾出空位后插入
```

最坏情况：在头部插入，所有 n 个元素都要后移 → **O(n)**

## 动态扩容——均摊 O(1)

`push_back` 不是每次都要扩容。只有当 `size == capacity` 时才扩容（通常翻倍），此时需要复制所有元素 O(n)。

但因为它不是每次都发生，**均摊**下来每次 push_back 还是 O(1)。

## 关键要点

- 连续内存 → 随机访问 O(1)，插入/删除中间 O(n)
- 末尾操作 O(1)，头部操作 O(n)
- 扩容策略：每次翻倍，均摊 O(1)
- C++ 中 `std::vector` 就是这个实现
