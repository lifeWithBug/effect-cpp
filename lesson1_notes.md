# 第 1 课：时间复杂度与空间复杂度

## 为什么需要复杂度分析？

判断算法好坏，不看代码长短，看**当数据量变大时，算法能不能撑住**。

复杂度用数学语言描述"代码跑得多快、吃多少内存"。

## 大 O 表示法

大 O 不关心精确秒数（取决于 CPU），它关心的是**数据规模 n 增长时，操作次数增长的趋势**。

```
O(1)      常数时间  — 不管 n 多大，一步完成
O(log n)  对数时间  — 每次把问题规模砍半
O(n)      线性时间  — 数据翻倍，时间翻倍
O(n log n)线性对数  — 高效排序的标配
O(n²)     平方时间  — 数据加 10 倍，时间加 100 倍
O(2ⁿ)     指数时间  — 增长爆炸，n=20 就天荒地老
```

### 直观对比

假设每步耗时 1 纳秒：

| n | O(1) | O(log n) | O(n) | O(n²) | O(2ⁿ) |
|---|------|----------|------|-------|-------|
| 10 | 1ns | 3ns | 10ns | 100ns | 1μs |
| 100 | 1ns | 7ns | 100ns | 10μs | 10¹⁴ 年 |
| 1000 | 1ns | 10ns | 1μs | 1ms | — |
| 100万 | 1ns | 20ns | 1ms | 17min | — |

O(2ⁿ) 在 n=100 时宇宙都毁灭了，暴力穷举很多时候行不通。

## 常见规则

### 1. 取最高次项

```
O(2n² + 3n + 100) → O(n²)
```

n 足够大时，低次项和常数可以忽略。

### 2. 乘法：嵌套循环

```cpp
for (int i = 0; i < n; i++)      // O(n)
    for (int j = 0; j < n; j++)  // O(n)
        doSomething();           // O(1)
// 总共 O(n * n) = O(n²)
```

### 3. 加法：并列循环

```cpp
for (int i = 0; i < n; i++)  // O(n)
    doA();
for (int i = 0; i < n; i++)  // O(n)
    doB();
// 总共 O(n + n) = O(n)，不是 O(n²)
```

## 空间复杂度

同样的规则，但分析的是**额外分配了多少内存**（不算输入本身）。

```cpp
int sum(int arr[], int n) {
    int total = 0;      // 只用了 1 个变量 → O(1) 空间
    for (int i = 0; i < n; i++)
        total += arr[i];
    return total;
}

int* double_arr(int arr[], int n) {
    int* result = new int[n];  // 分配了 n 大小 → O(n) 空间
    for (int i = 0; i < n; i++)
        result[i] = arr[i] * 2;
    return result;
}
```

## 复杂度排行

**O(1) < O(log n) < O(n) < O(n log n) < O(n²) < O(2ⁿ)**

## 记忆口诀

- 嵌套循环 → 各项复杂度相乘
- 并列循环 → 各项复杂度相加
- 只看最高次项，忽略常数和低次项
- 空间复杂度算的是"额外"开销，输入数据本身不算
